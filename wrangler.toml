# Cloudflare Pages configuration
# (NOT a Workers config — do not add an [assets] block, that switches the
# project to Workers mode and Pages Functions in /functions are ignored.)
name = "skylight-media-solutions"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./dist"
